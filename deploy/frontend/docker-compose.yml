services:
  frontend:
    build:
      context: ../../frontend
      args:
        VITE_VIDEO_API_URL: ${VITE_VIDEO_API_URL:-http://localhost}
        VITE_API_URL: ${VITE_API_URL:-}
    ports:
      - "80:80"
    restart: unless-stopped
